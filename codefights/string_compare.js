/*
  I think I need to for loop through the first one.
  Then an inner loop to compare the current index to the second string?
*/

/*
  function findFirstSubstringOccurrence(s, x) {
    return s.indexOf(x);
  }
*/i

  function findFirstSubstringOccurrence(s, x) {
    
}
